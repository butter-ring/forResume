/*
 * OrderSell Messages
 *
 * This contains all the text for the OrderSell component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.OrderSell';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the OrderSell component!',
  },
});

/*
 * TokenList Messages
 *
 * This contains all the text for the TokenList component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.TokenList';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TokenList component!',
  },
});

/*
 * TokenInfo Messages
 *
 * This contains all the text for the TokenInfo component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.TokenInfo';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TokenInfo component!',
  },
});

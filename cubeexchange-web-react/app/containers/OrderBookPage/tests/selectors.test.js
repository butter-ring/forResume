// import { selectOrderBookPageDomain } from '../selectors';

describe('selectOrderBookPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});

/*
 * BoardTabImage Messages
 *
 * This contains all the text for the BoardTabImage component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.BoardTabImage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the BoardTabImage component!',
  },
});

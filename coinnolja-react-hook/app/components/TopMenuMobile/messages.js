/*
 * TopMenuMobile Messages
 *
 * This contains all the text for the TopMenuMobile component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.TopMenuMobile';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TopMenuMobile component!',
  },
});

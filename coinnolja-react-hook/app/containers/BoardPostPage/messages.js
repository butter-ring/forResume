/*
 * BoardPostPage Messages
 *
 * This contains all the text for the BoardPostPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.BoardPostPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the BoardPostPage container!',
  },
});

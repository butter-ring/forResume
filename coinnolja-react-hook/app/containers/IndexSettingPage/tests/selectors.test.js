// import { selectIndexSettingPageDomain } from '../selectors';

describe('selectIndexSettingPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});

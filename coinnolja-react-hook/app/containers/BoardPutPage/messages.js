/*
 * BoardPutPage Messages
 *
 * This contains all the text for the BoardPutPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.BoardPutPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the BoardPutPage container!',
  },
});

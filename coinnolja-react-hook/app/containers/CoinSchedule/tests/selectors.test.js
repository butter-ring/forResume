// import { selectCoinScheduleDomain } from '../selectors';

describe('selectCoinScheduleDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});

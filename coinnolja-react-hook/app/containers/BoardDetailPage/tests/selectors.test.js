// import { selectBoardDetailPageDomain } from '../selectors';

describe('selectBoardDetailPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
